server:
  port: 9003

spring:
  datasource:
    url: jdbc:postgresql://localhost:5434/order_db?useSSL=false
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update

  kafka:
    bootstrap-servers: localhost:9092,localhost:9093,localhost:9094

