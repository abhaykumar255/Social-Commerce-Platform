server:
  port: 9002

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/product_db?useSSL=false
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver

  redis:
    host: localhost
    port: 6379
