# Docker Development Profile
spring:
  data:
    redis:
      host: redis
      port: 6379

eureka:
  client:
    service-url:
      defaultZone: http://commerce-service-discovery:8761/eureka/

logging:
  level:
    org.springframework.cloud.gateway: INFO
    com.social.gateway: INFO

